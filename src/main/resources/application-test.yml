spring:
  application:
    name: fint-consumer-skeleton

fint:
  events:
    test-mode: true
    queue-endpoint-enabled: true

  audit:
    test-mode: true

  relations:
    force-https: false
    default-base-url: /

  cache:
    manager: hazelcast

  hazelcast:
    members: localhost

server:
  context-path: ""
  port: 8080
